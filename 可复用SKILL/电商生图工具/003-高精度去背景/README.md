# 高精度去背景 — 用 BiRefNet 模型高精度去除图片背景，生成透明 PNG

## 功能

用 BiRefNet 模型高精度去除图片背景，生成透明 PNG。适用于产品图、人像、电商图抠图。

## 用法

```bash
infsh app run infsh/birefnet --input '{"image_url":"https://..."}'
```

## 依赖

inference.sh CLI

---

*详情请参见「参考文档」目录下的 SKILL.md 和 用户说明.md*
